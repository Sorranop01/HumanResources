# Employee Data Structure - Complete Proposal
> การออกแบบโครงสร้างข้อมูลพนักงานแบบครบถ้วนสำหรับระบบ HR

## 📊 ภาพรวมโครงสร้างข้อมูล

### 1. ข้อมูลส่วนบุคคล (Personal Information)

```typescript
interface PersonalInfo {
  // ข้อมูลพื้นฐาน
  firstName: string;                    // ชื่อ (EN)
  lastName: string;                     // นามสกุล (EN)
  thaiFirstName: string;               // ชื่อ (TH)
  thaiLastName: string;                // นามสกุล (TH)
  nickname?: string;                   // ชื่อเล่น

  // ข้อมูลติดต่อ
  email: string;                       // อีเมลหลัก
  personalEmail?: string;              // อีเมลส่วนตัว
  phoneNumber: string;                 // เบอร์โทรศัพท์
  emergencyContact: {
    name: string;
    relationship: string;              // ความสัมพันธ์ (พ่อ, แม่, คู่สมรส)
    phoneNumber: string;
  };

  // ข้อมูลส่วนตัว
  dateOfBirth: Date;                   // วันเกิด
  age: number;                         // อายุ (คำนวณอัตโนมัติ)
  gender: 'male' | 'female' | 'other'; // เพศ
  maritalStatus: 'single' | 'married' | 'divorced' | 'widowed'; // สถานภาพสมรส
  nationality: string;                 // สัญชาติ
  religion?: string;                   // ศาสนา

  // ข้อมูลบัตรประชาชน
  nationalId: string;                  // เลขบัตรประชาชน
  nationalIdIssueDate?: Date;          // วันออกบัตร
  nationalIdExpiryDate?: Date;         // วันหมดอายุบัตร

  // ที่อยู่
  address: {
    current: Address;                  // ที่อยู่ปัจจุบัน
    permanent?: Address;               // ที่อยู่ตามทะเบียนบ้าน
  };

  // รูปภาพ
  photoURL?: string;                   // รูปโปรไฟล์
}

interface Address {
  addressLine1: string;                // ที่อยู่บรรทัดที่ 1
  addressLine2?: string;               // ที่อยู่บรรทัดที่ 2
  subDistrict: string;                 // ตำบล/แขวง
  district: string;                    // อำเภอ/เขต
  province: string;                    // จังหวัด
  postalCode: string;                  // รหัสไปรษณีย์
  country: string;                     // ประเทศ
}
```

### 2. ข้อมูลการจ้างงาน (Employment Information)

```typescript
interface EmploymentInfo {
  // ข้อมูลพนักงาน
  employeeCode: string;                // รหัสพนักงาน (เช่น EMP-2025-001)

  // วันที่สำคัญ
  hireDate: Date;                      // วันเริ่มงาน ✅ มีแล้ว
  probationEndDate?: Date;             // วันสิ้นสุดทดลองงาน
  confirmationDate?: Date;             // วันผ่านทดลองงาน
  terminationDate?: Date;              // วันสิ้นสุดการจ้างงาน ⭐ NEW
  lastWorkingDate?: Date;              // วันทำงานวันสุดท้าย

  // สถานะการจ้างงาน
  status: EmployeeStatus;              // active, on-leave, resigned, terminated
  employmentType: EmploymentType;      // ประเภทการจ้างงาน ⭐ NEW
  workType: WorkType;                  // รูปแบบการทำงาน ⭐ NEW

  // โครงสร้างองค์กร
  position: string;                    // ตำแหน่งงาน ✅ มีแล้ว
  level?: string;                      // ระดับ (Junior, Senior, Manager, etc.)
  department: string;                  // แผนก ✅ มีแล้ว
  division?: string;                   // ฝ่าย
  team?: string;                       // ทีม

  // สายการบังคับบัญชา
  reportingTo?: {
    employeeId: string;                // รหัสพนักงานหัวหน้า
    employeeName: string;              // ชื่อหัวหน้า (denormalized)
    position: string;                  // ตำแหน่งหัวหน้า
  };

  // สถานที่ทำงาน
  workLocation: {
    office: string;                    // สำนักงาน (กรุงเทพ, เชียงใหม่)
    building?: string;                 // อาคาร
    floor?: string;                    // ชั้น
    seat?: string;                     // ที่นั่ง
  };
}

// ⭐ NEW: ประเภทการจ้างงาน
type EmploymentType =
  | 'permanent'        // พนักงานประจำ
  | 'contract'         // พนักงานสัญญาจ้าง
  | 'probation'        // พนักงานทดลองงาน
  | 'freelance'        // ฟรีแลนซ์
  | 'intern';          // พนักงานฝึกงาน

// ⭐ NEW: รูปแบบการทำงาน (Full-time/Part-time)
type WorkType =
  | 'full-time'        // เต็มเวลา ⭐ ที่คุณถาม
  | 'part-time';       // นอกเวลา ⭐ ที่คุณถาม
```

### 3. ข้อมูลค่าตอบแทนและสวัสดิการ (Compensation & Benefits)

```typescript
interface CompensationInfo {
  // เงินเดือนและค่าจ้าง
  salary: {
    baseSalary: number;                // เงินเดือนพื้นฐาน ✅ มีแล้ว (แยกออกมาเป็น object)
    currency: string;                  // สกุลเงิน (THB, USD)
    paymentFrequency: 'monthly' | 'bi-weekly' | 'weekly' | 'hourly'; // รอบการจ่าย
    effectiveDate: Date;               // วันที่มีผลบังคับใช้

    // ค่าจ้างสำหรับ Part-time
    hourlyRate?: number;               // อัตราค่าจ้างต่อชั่วโมง ⭐ สำหรับ part-time
  };

  // ค่าตอบแทนเพิ่มเติม
  allowances?: Array<{                 // เบี้ยเลี้ยง/ค่าตอบแทนพิเศษ
    type: string;                      // ประเภท (ค่าเดินทาง, ค่าโทรศัพท์, ค่าตำแหน่ง)
    amount: number;                    // จำนวนเงิน
    frequency: 'monthly' | 'quarterly' | 'yearly'; // ความถี่
  }>;

  // ประวัติการปรับเงินเดือน
  salaryHistory?: Array<{
    effectiveDate: Date;               // วันที่มีผล
    previousSalary: number;            // เงินเดือนเดิม
    newSalary: number;                 // เงินเดือนใหม่
    increasePercentage: number;        // เปอร์เซ็นต์ที่เพิ่มขึ้น
    reason?: string;                   // เหตุผล (ปรับตามผลงาน, โปรโมชั่น)
    approvedBy?: string;               // ผู้อนุมัติ
  }>;

  // โบนัส
  bonus?: {
    annualBonus?: number;              // โบนัสประจำปี
    performanceBonus?: number;         // โบนัสตามผลงาน
    otherBonus?: number;               // โบนัสอื่นๆ
  };

  // สวัสดิการ
  benefits?: {
    healthInsurance: boolean;          // ประกันสุขภาพ
    lifeInsurance: boolean;            // ประกันชีวิต
    providentFund: {                   // กองทุนสำรองเลี้ยงชีพ
      isEnrolled: boolean;
      employeeContributionRate?: number; // % ที่พนักงานจ่าย
      employerContributionRate?: number; // % ที่บริษัทจ่าย
    };
    annualLeave: number;               // วันลาพักร้อน (วัน/ปี)
    sickLeave: number;                 // วันลาป่วย (วัน/ปี)
    otherBenefits?: string[];          // สวัสดิการอื่นๆ
  };
}
```

### 4. ข้อมูลภาษีและประกันสังคม (Tax & Social Security)

```typescript
interface TaxAndSocialSecurityInfo {
  // ประกันสังคม
  socialSecurity: {
    isEnrolled: boolean;               // ⭐ เข้าประกันสังคมหรือไม่ (ที่คุณถาม)
    ssNumber?: string;                 // เลขประกันสังคม
    enrollmentDate?: Date;             // วันที่เริ่มเข้าร่วม
    hospitalCode?: string;             // รหัสโรงพยาบาลประจำ
    hospitalName?: string;             // ชื่อโรงพยาบาล
  };

  // ภาษี
  tax: {
    taxId?: string;                    // เลขประจำตัวผู้เสียภาษี
    withholdingTax: boolean;           // ⭐ หัก ณ ที่จ่ายหรือเปล่า (ที่คุณถาม)
    withholdingRate?: number;          // อัตราการหัก (%)
    taxReliefs?: Array<{               // ลดหย่อนภาษี
      type: string;                    // ประเภท (บิดา, มารดา, คู่สมรส, บุตร, ประกันชีวิต)
      amount: number;                  // จำนวนเงิน
    }>;
  };

  // บัญชีธนาคาร (สำหรับโอนเงินเดือน)
  bankAccount: {
    bankName: string;                  // ชื่อธนาคาร
    accountNumber: string;             // เลขที่บัญชี
    accountName: string;               // ชื่อบัญชี
    branchName?: string;               // สาขา
  };
}
```

### 5. ข้อมูลการศึกษาและประสบการณ์ (Education & Experience)

```typescript
interface EducationAndExperienceInfo {
  // การศึกษา
  education: Array<{
    level: 'high-school' | 'diploma' | 'bachelor' | 'master' | 'doctorate';
    institution: string;               // สถาบัน
    fieldOfStudy: string;              // สาขา
    graduationYear: number;            // ปีที่สำเร็จการศึกษา
    gpa?: number;                      // เกรดเฉลี่ย
  }>;

  // ใบอนุญาตประกอบวิชาชีพ
  certifications?: Array<{
    name: string;                      // ชื่อใบอนุญาต/ใบรับรอง
    issuingOrganization: string;       // หน่วยงานที่ออก
    issueDate: Date;                   // วันที่ออก
    expiryDate?: Date;                 // วันหมดอายุ
    credentialId?: string;             // รหัสหนังสือรับรอง
  }>;

  // ทักษะ
  skills?: Array<{
    name: string;                      // ชื่อทักษะ
    level: 'beginner' | 'intermediate' | 'advanced' | 'expert';
    yearsOfExperience?: number;        // จำนวนปีประสบการณ์
  }>;

  // ภาษา
  languages?: Array<{
    language: string;                  // ภาษา
    proficiency: 'basic' | 'intermediate' | 'advanced' | 'native';
    canRead: boolean;
    canWrite: boolean;
    canSpeak: boolean;
  }>;
}
```

### 6. ข้อมูลเอกสารและไฟล์แนบ (Documents)

```typescript
interface DocumentsInfo {
  documents?: Array<{
    type: DocumentType;                // ประเภทเอกสาร
    fileName: string;                  // ชื่อไฟล์
    fileURL: string;                   // URL ของไฟล์
    uploadDate: Date;                  // วันที่อัปโหลด
    uploadedBy: string;                // ผู้อัปโหลด
    expiryDate?: Date;                 // วันหมดอายุ (ถ้ามี)
  }>;
}

type DocumentType =
  | 'resume'                           // ประวัติส่วนตัว
  | 'national-id'                      // สำเนาบัตรประชาชน
  | 'house-registration'               // สำเนาทะเบียนบ้าน
  | 'transcript'                       // ใบรับรองผลการศึกษา
  | 'degree-certificate'               // ใบปริญญา
  | 'work-permit'                      // ใบอนุญาตทำงาน (สำหรับชาวต่างชาติ)
  | 'contract'                         // สัญญาจ้าง
  | 'nda'                             // สัญญาความลับ
  | 'photo'                           // รูปถ่าย
  | 'other';                          // อื่นๆ
```

### 7. ข้อมูลเวลาทำงาน (Work Schedule)

```typescript
interface WorkScheduleInfo {
  // รูปแบบการทำงาน
  workSchedule: {
    scheduleType: 'fixed' | 'flexible' | 'shift'; // รูปแบบกะการทำงาน

    // สำหรับ Fixed Schedule
    standardHours?: {
      monday?: WorkHours;
      tuesday?: WorkHours;
      wednesday?: WorkHours;
      thursday?: WorkHours;
      friday?: WorkHours;
      saturday?: WorkHours;
      sunday?: WorkHours;
    };

    // สำหรับ Shift Work
    currentShift?: {
      shiftName: string;               // ชื่อกะ (กะเช้า, กะบ่าย, กะดึก)
      startTime: string;               // เวลาเริ่ม
      endTime: string;                 // เวลาสิ้นสุด
    };

    // จำนวนชั่วโมงทำงานมาตรฐาน
    hoursPerWeek: number;              // ชั่วโมง/สัปดาห์ (40 สำหรับ full-time)
    hoursPerDay: number;               // ชั่วโมง/วัน (8 สำหรับ full-time)
  };

  // OT (ล่วงเวลา)
  overtime: {
    isEligible: boolean;               // มีสิทธิ์ได้ OT หรือไม่
    rate: number;                      // อัตราจ่าย OT (1.5x, 2x, 3x)
  };
}

interface WorkHours {
  startTime: string;                   // เช่น "09:00"
  endTime: string;                     // เช่น "18:00"
  breakMinutes: number;                // เวลาพัก (นาที)
}
```

### 8. ข้อมูลประสิทธิภาพและการพัฒนา (Performance & Development)

```typescript
interface PerformanceInfo {
  // ประเมินผลงาน
  performanceReviews?: Array<{
    reviewDate: Date;                  // วันที่ประเมิน
    reviewPeriod: string;              // งวดที่ประเมิน (Q1 2025)
    rating: number;                    // คะแนน (1-5)
    strengths?: string;                // จุดแข็ง
    areasForImprovement?: string;      // จุดที่ควรพัฒนา
    goals?: string;                    // เป้าหมาย
    reviewedBy: string;                // ผู้ประเมิน
  }>;

  // การอบรม
  trainings?: Array<{
    trainingName: string;              // ชื่อหัวข้อการอบรม
    trainingDate: Date;                // วันที่อบรม
    duration: number;                  // ระยะเวลา (ชั่วโมง)
    provider?: string;                 // ผู้จัด
    certificate?: string;              // URL ใบประกาศ
    cost?: number;                     // ค่าใช้จ่าย
  }>;

  // การเลื่อนตำแหน่ง
  promotionHistory?: Array<{
    effectiveDate: Date;               // วันที่มีผล
    fromPosition: string;              // ตำแหน่งเดิม
    toPosition: string;                // ตำแหน่งใหม่
    reason?: string;                   // เหตุผล
    approvedBy?: string;               // ผู้อนุมัติ
  }>;
}
```

---

## 🏗️ โครงสร้างข้อมูลที่แนะนำ (Recommended Structure)

### แนวทาง 1: แยกเป็น Collections ย่อย (Subcollections) - แนะนำ ⭐

```
employees/{employeeId}
  ├── (main document) - ข้อมูลหลัก + ข้อมูลที่ query บ่อย
  ├── /compensation (subcollection) - ข้อมูลค่าตอบแทน
  ├── /documents (subcollection) - เอกสารแนบ
  ├── /education (subcollection) - ประวัติการศึกษา
  ├── /performance (subcollection) - ประเมินผลงาน
  └── /trainings (subcollection) - การอบรม
```

**ข้อดี:**
- ข้อมูลหลักเล็กและเร็ว
- แยกข้อมูลที่มีความละเอียดอ่อน (เงินเดือน, ภาษี)
- รองรับข้อมูลที่มีจำนวนมาก (เอกสาร, การอบรม)
- Security rules ละเอียดขึ้น

**ข้อเสีย:**
- Query ซับซ้อนขึ้นถ้าต้องการข้อมูลหลายส่วนพร้อมกัน

### แนวทาง 2: Flat Structure with References

```
employees/{employeeId} - ข้อมูลหลัก
compensations/{compensationId} - ข้อมูลค่าตอบแทน (employeeId reference)
documents/{documentId} - เอกสาร (employeeId reference)
...
```

---

## 🎯 แผนการพัฒนาแนะนำ (Implementation Plan)

### Phase 1: ข้อมูลพื้นฐานเพิ่มเติม (Quick Wins) ⚡
1. **Employment Type & Work Type**
   - เพิ่ม `employmentType` (permanent, contract, probation, etc.)
   - เพิ่ม `workType` (full-time, part-time)

2. **วันที่สำคัญ**
   - เพิ่ม `terminationDate`, `probationEndDate`
   - เพิ่ม `lastWorkingDate`

3. **ข้อมูลค่าจ้างเพิ่มเติม**
   - แยก `salary` เป็น object มี `baseSalary`, `currency`, `paymentFrequency`
   - เพิ่ม `hourlyRate` สำหรับ part-time

### Phase 2: ภาษีและประกันสังคม 🏦
1. **Social Security**
   - เพิ่ม `socialSecurity` object
   - `isEnrolled`, `ssNumber`, `hospitalCode`

2. **Tax Information**
   - เพิ่ม `tax` object
   - `withholdingTax`, `withholdingRate`, `taxReliefs`

3. **Bank Account**
   - เพิ่ม `bankAccount` สำหรับโอนเงินเดือน

### Phase 3: ข้อมูลเชิงลึก 📊
1. **Work Schedule**
   - เพิ่ม `workSchedule` และ `overtime`

2. **Allowances & Benefits**
   - เพิ่ม `allowances`, `benefits`

3. **Documents Management**
   - เพิ่ม subcollection สำหรับเอกสารแนบ

### Phase 4: Performance & Development 🎓
1. **Performance Reviews**
2. **Training Records**
3. **Promotion History**

---

## 💡 คำแนะนำการใช้งาน

### สำหรับข้อมูลที่ sensitive (เงินเดือน, ภาษี)
```typescript
// ควรใช้ Firestore Security Rules
match /employees/{employeeId}/compensation/{doc} {
  // เฉพาะ HR, Admin และเจ้าของข้อมูลเท่านั้น
  allow read: if isHR() || isAdmin() || isOwner(employeeId);
  allow write: if isHR() || isAdmin();
}
```

### การ Query ข้อมูลที่มีประสิทธิภาพ
```typescript
// Index ที่แนะนำ
- status + employmentType + department
- hireDate (desc)
- department + position
```

---

## 📝 สรุป

ข้อมูลที่**ขาดและควรเพิ่มเร็วที่สุด** (ตามที่คุณถาม):
1. ✅ `terminationDate` - สิ้นสุดการจ้างงาน
2. ✅ `employmentType` - ประเภทการจ้างงาน (permanent, contract, etc.)
3. ✅ `workType` - full-time / part-time
4. ✅ `hourlyRate` - ค่าจ้างต่อชั่วโมง (สำหรับ part-time)
5. ✅ `socialSecurity.isEnrolled` - เข้าประกันสังคมหรือไม่
6. ✅ `tax.withholdingTax` - หัก ณ ที่จ่ายหรือเปล่า

คุณต้องการให้ฉันเริ่มพัฒนาส่วนไหนก่อนครับ?
