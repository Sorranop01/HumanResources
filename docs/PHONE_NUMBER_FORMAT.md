# รูปแบบเบอร์โทรศัพท์ที่รองรับ

ระบบรองรับการใส่เบอร์โทรศัพท์หลายรูปแบบและจะแปลงเป็นรูปแบบ E.164 อัตโนมัติ

## ✅ รูปแบบเบอร์ไทยที่รองรับ

### เบอร์มือถือ (Mobile)
```
0812345678          → แปลงเป็น +66812345678
081-234-5678        → แปลงเป็น +66812345678
081 234 5678        → แปลงเป็น +66812345678
+66812345678        → ใช้ได้เลย (E.164)
66812345678         → แปลงเป็น +66812345678
```

รองรับเบอร์ที่ขึ้นต้นด้วย:
- `06` (AIS, dtac, TrueMove H)
- `08` (AIS, dtac, TrueMove H)
- `09` (AIS, dtac, TrueMove H)

### เบอร์บ้าน (Landline)
```
022345678           → แปลงเป็น +6622345678 (กรุงเทพ)
0312345678          → แปลงเป็น +66312345678 (สระบุรี)
044123456           → แปลงเป็น +6644123456 (ขอนแก่น)
```

รองรับเบอร์ที่ขึ้นต้นด้วย:
- `02` (กรุงเทพและปริมณฑล)
- `03` (ภาคกลาง/ตะวันออก)
- `04` (ภาคตะวันออกเฉียงเหนือ)
- `05` (ภาคเหนือ)

## 🌍 รูปแบบต่างประเทศ

ใช้รูปแบบ E.164 มาตรฐานสากล:
```
+12125551234        → USA
+447911123456       → UK
+85212345678        → Hong Kong
+6531234567         → Singapore
```

## ❌ รูปแบบที่ไม่รองรับ

```
81-234-5678         ❌ (ไม่มี 0 หน้า)
812345678           ❌ (ไม่มี 0 หน้า)
081234567           ❌ (จำนวนหลักไม่ถูกต้อง)
08123456789         ❌ (เกิน 10 หลัก)
```

## 🔧 การใช้งาน

### ใน UI (Form)
```tsx
// ผู้ใช้สามารถพิมพ์เบอร์แบบไหนก็ได้
<Input
  placeholder="เบอร์โทรศัพท์ เช่น 0812345678"
  value={phoneNumber}
/>
```

### ใน Cloud Functions
```typescript
import { preparePhoneNumberForAuth } from './utils/phoneNumber';

// แปลงเบอร์อัตโนมัติ
const normalizedPhone = preparePhoneNumberForAuth('0812345678');
// ได้ผลลัพธ์: '+66812345678'
```

### ใน Seed Scripts
```typescript
// ใช้เบอร์แบบไทยได้เลย
const user = {
  phoneNumber: '0812345678', // จะถูกแปลงอัตโนมัติ
};
```

## 📱 ตัวอย่างการใช้งานจริง

### เพิ่ม User ใหม่
```json
{
  "email": "user@example.com",
  "displayName": "John Doe",
  "phoneNumber": "0812345678"  ← ใส่แบบนี้ได้เลย!
}
```

### Response
```json
{
  "success": true,
  "user": {
    "phoneNumber": "+66812345678"  ← ถูกแปลงเป็น E.164 อัตโนมัติ
  }
}
```

## 💡 Tips

1. **แนะนำ:** ใช้เบอร์แบบไทยปกติ `0812345678` ง่ายและสะดวก
2. **ไม่ต้องกรอก:** ถ้าไม่มีเบอร์โทร สามารถข้ามได้
3. **Auto-format:** ระบบจะจัดรูปแบบให้อัตโนมัติ ไม่ต้องกังวล
4. **Validation:** ระบบจะตรวจสอบความถูกต้องให้อัตโนมัติ

## 🧪 ทดสอบ

ลองสร้าง user ด้วยเบอร์เหล่านี้:
- ✅ `0812345678`
- ✅ `081-234-5678`
- ✅ `+66812345678`
- ✅ ไม่ใส่เบอร์
